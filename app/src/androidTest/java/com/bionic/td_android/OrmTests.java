package com.bionic.td_android;

import android.test.AndroidTestCase;

/**
 * Created by user on 30.03.2016.
 */
public class OrmTests extends AndroidTestCase {



    public void test() {
//        DbManager manager = new DbManager(getContext());
//
//        Shift loaded = manager.loadLocalShift();
//        if(loaded!= null)Log.e("Bionic", "Loaded shift : " + loaded.toString());
//        else Log.e("Bionic", "Error loading shift");
//
//        new ShiftSelection().delete(getContext());
//        new RideSelection().delete(getContext());
//
//        loaded = manager.loadLocalShift();
//        if(loaded!= null)Log.e("Bionic", "Loaded shift : " + loaded.toString());
//        else Log.e("Bionic", "Error loading shift");
//
//
//        Shift shift = new Shift();
//        shift.setStartTime(new Date(0l));
//        shift.setEndTime(new Date(100000000l));
//
//        List<Ride> list  = new ArrayList<>();
//        list.add(Ride.createDummyRide());
//
//        shift.setRides(list);
//
//        if(shift.validate()){
//            shift.recountPause();
//            Log.e("Bionic", "Nice shift : " + shift.toString());
//
//            manager.saveUpdateLocalShift(shift);
//            loaded = manager.loadLocalShift();
//            if(loaded!= null)Log.e("Bionic", "Loaded shift : " + loaded.toString());
//            else Log.e("Bionic", "Error loading shift");
//        };

     //   new ShiftSelection().delete(getContext());
      //  new RideSelection().delete(getContext());
    }

}
